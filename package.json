{
  "name": "jli-declutter",
  "version": "1.4.1",
  "description": "Browser extension for collapsing LinkedIn feed and notification clutter.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "check": "node --check scripts/build-zip.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8'))\"",
    "zip": "node scripts/build-zip.js",
    "build": "npm run check && npm run zip"
  }
}
