{
  "name": "jli-declutter",
  "version": "1.3.0",
  "description": "CSS-only Chrome extension for hiding and collapsing LinkedIn clutter.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "generate:css-generator": "node scripts/generate-css-generator.js",
    "check": "node --check scripts/build-zip.js && node --check scripts/generate-css-generator.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8'))\"",
    "zip": "node scripts/build-zip.js",
    "build": "npm run generate:css-generator && npm run check && npm run zip"
  }
}
